# 当前接续状态 · 2026-09-22

用户提供的原始交接文档保存于 [v0.1.0-original.md](v0.1.0-original.md)。它描述原开发机及已发布 v0.1.0 的历史状态；文中的旧路径、进程选择、主窗口预览及仅点击触发描述，不替代本会话中的最新需求。

## 最新：按内容自适应高度与分页

- 用户再次明确：尽量不要滚动条，高度根据匹配结果数量伸缩，不能写死。已移除 `Settings.Height` / `AutoHeight` 与高度输入；旧 JSON 对应字段忽略。
- 浮窗按全部候选自然高度测量；超过当前屏幕工作区才分页，保持大图、完整候选与完整分母。重复相同候选不会重置当前页，候选变少会再次收缩。未新增音频回放、样本对比或录音导出，上一轮这些只是建议。
- 浮窗 Ctrl+Alt+PgUp / PgDn 翻页，仅监听开启、游戏前台、多页时注册；冲突提示在诊断中，也可从主窗口或历史按钮翻页。鼠标穿透与不抢焦点保留。
- 历史记录列表分页，详情也支持分页；历史、离线结果、图鉴均使用内容高度。增长时调整位置避免底部超出屏幕。主窗口设置改成五个分区，取消长滚动页面。
- 15 项控制器与布局回归通过（原 12 项加 3 项布局测试）；测量覆盖 300/470/900 宽、大图与名称、全 51 件分页可达、重复匹配保页和重新收缩。UI 检查覆盖 100 条历史全部可达、单件/多件高度差、主窗口各分区及 672 高度限制。
- 当前包：`artifacts/adaptive-layout` 与 `artifacts/AqtwListener-adaptive-layout-win-x64.zip`；上一版保留。音效库、匹配算法与结果保留逻辑本轮未改变。报告：`docs/measurements/adaptive-layout-tests.json`、`adaptive-ui-smoke.json`。

## 前一版：结果保留、历史、大图与素材复核

- 用户已反馈声音自动识别「效果不错」，另报琥珀天星、双颈琴无反应和结果一闪而过。最新要求图片放大、浮窗高度自动调整并尽量不滚动。
- 已接受的有效匹配一直保留到新的有效匹配或手动清除，显示匹配时间。静音、分析中、失败、切出、关闭监听不删除已接受结果；正在计算的旧任务仍取消并丢弃迟到结果。
- 主窗口增加「识别历史」和「清除当前结果」。历史独立窗口展示本次运行最近 100 条，有时间、来源、完整同音候选和占比；相邻自动重复合并、同次点击两阶段合并。只存内存，重启清空。清历史与清浮窗互不影响。
- 图片默认 88（原 60），设置可调 60–120；默认自动高度，关闭后可设固定高度。小格数组并排，超出屏幕工作区才滚动；主窗口仍无预览。绿色边框代替每卡重复的「最匹配」行。
- 旧 importer 对两组的「放下冲突」硬编码隔离不成立：原始拿起文件与当前参考哈希一致，与放下文件不同。已恢复定位组、琥珀天心类并重建两个索引；当前 9 组 / 14 段 / 51 件候选，库版本 `0.1.1-pickup-source-fix`。阈值和 Go 引擎未改。复核证据见 `docs/COVERAGE-REVIEW.md`。
- 「琥珀天星」与「琥珀天心」名称对应待实机确认；「双颈琴」不在上游原始/合并库和本地目录中，不能视作「里拉琴」，尚需真实物品/音效资料。没有声称修复该物品。
- 24 核心 + 12 控制器测试通过，另有 22 参考/派生窗口回归通过；UI 渲染与自适应高度检查通过。它们不等于实战准确率验证。报告在 `docs/measurements/history-*.json` 与 `coverage-reference-tests.json`。
- 最新目录 `artifacts/history-preview`，便携包 `artifacts/AqtwListener-history-preview-win-x64.zip`。原 `audio-fallback` 保留，本轮不替换或停止用户旧程序，也没有推送远程。切换需用户退出旧版后启动新版。

以下记录为前一版声音识别修复经过，涉及自动清除、25 件覆盖等描述已被上述最新需求和复核结果替代。

## 之前工作区与用户确认的变化

- 本地项目：`C:\Users\Dexin\Documents\ChatGPT\暗区行商助手`。基线提交：`5ae871cc7df445d43206b0ad82364a098e13f7f2`。本轮改动仍在工作区，尚未提交或推送。
- 继续使用 WPF/.NET 10、NAudio 回环与常驻 SoundRadar Go 引擎，未改写 DSP、索引或候选匹配阈值。
- 用户明确要求：固定暗区突围 `UAGame` 进程，移除主窗口浮窗预览。已实现单栏设置窗口；游戏内浮窗保留。
- 用户通过 UU 远程操作，报告仓库拖动无响应。最新截图显示正在监听、已收到声音，但鼠标触发次数为 0。可以确定该次尚未启动点击识别，具体输入缺失原因未确认。
- 鼠标按键轮询、事件去重及丢失松开后的恢复已加入，但用户实际截图表明仍未解决点击触发。因此新增默认开启、可关闭的声音自动识别：前台且有声音时扫描重叠音频窗口，不依赖鼠标事件；鼠标触发仍优先。
- 声音模式增加分析次数、状态和诊断导出，切出/关闭时取消旧结果，最近自动匹配候选会过期。它改变了原版仅点击计算的负载与声音范围，需要单独验证性能与误报。

## 验证与交付

- 20 项核心测试通过，见 `docs/measurements/audio-fallback-tests.json`。
- 8 项控制器/引擎测试通过，见 `docs/measurements/audio-fallback-controller-tests.json`。包括零鼠标输入触发、实际 SoundRadar 工作进程返回判定、忙碌限流、切出/关闭后丢弃旧结果及候选过期。
- 编译、默认开启选项、固定进程、单栏渲染、浮窗穿透/不抢焦点检查通过。
- 当前程序目录：`artifacts/audio-fallback`；测试包：`artifacts/AqtwListener-audio-fallback-win-x64.zip`。
- 本机 .NET SDK 为项目内 `.tools/dotnet` 10.0.401。本轮未安装 Go SDK、未重编译 Go 核心；引擎复用原 v0.1.0 便携包二进制，哈希记录在包内 `build-info.json`，目录与声纹索引校验一致。
- 最新声音模式尚未收到用户的实机反馈；28 项工程测试不等于实战准确率验收，也不能证明所有 UU 输入方式兼容。
- 阅读交接文档时发现最新本地测试包缺少根目录 `LICENSE`，已补入，并在正式发布脚本中加入复制步骤。未改动 GitHub v0.1.0 Release。

## 保持的数据与产品边界

核对实际 `data/library/library.json`：51 件目录、7 个启用拾起音效组、12 条参考样本、25 件关联候选，仍为 `uncalibrated`；另外 26 件的放下来源冲突尚未解决。不能把任意枪械、弹药或库外物品的无候选判为触发失败。

继续保留全部同音候选、缩略图与格数分组。大金占比使用完整去重候选计算，空候选为“—”；没有单件概率，不编造回收价。正常使用仅短时内存音频，不保存录音、不上传、不读取游戏画面/内存/数据包、不自动购买。

后续以用户实际反馈为准，先区分“声音分析次数不增加”与“分析后未匹配”，再针对已收录物品验证。独立样本、校准、误报、实机端到端延迟和运行负载仍待完成；原交接文档中的静态性能和派生样本数据只保留为历史证据。
