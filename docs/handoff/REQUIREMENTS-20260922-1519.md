# 2026-09-22 15:19 起新增与重申的需求

整理范围从 15:19 的“不允许分页；试听音频功能哪去了？切换字号功能呢？”开始。以下按最终补充合并，作为后续待实施清单；整理不代表重新开始实施。

1. **不允许分页。** 不能把候选结果分到上一页、下一页，也不能用分页代替布局适配。
2. **补齐可用的音频回放与参考试听入口。** 参考图用于说明这些功能：回放刚刚采到的声音，试听候选对应的参考音效。入口应在浮窗中明确、易找，不能有功能却让用户找不到。
3. **提供字号切换。** 浮窗应能方便地切换小、中、大字号，实际影响显示。
4. **内容增多时优先横向利用空间。** 可以适当变宽，充分利用左侧背包与右侧货物之间的中间空白，尽量减少向下延展和对中央视野的遮挡。不要照搬截图的绝对坐标或固定死尺寸。
5. **尽量避免滚动。** 尽可能完整展示内容，减少滚动操作；这一要求与“不允许分页”同时成立。如何处理极端数量、分辨率和字号组合，尚未重新确认，不把临时实现当作最终要求。
6. **固定与解锁使用同一图钉的两种状态。** 固定时图钉亮起；解锁时显示灰暗、未亮起的图钉。无需“固定/移动”文字按钮，也不切换成四向箭头图标。灰暗表示未固定状态，不是禁用点击功能。
7. **只吸纳参考软件的功能，保留自己的设计。** 不照搬对方的整体布局、视觉样式和文案，不做成外观相同的软件。功能应融入本项目原有界面，而不是重制参考截图。

补充说明：用户重新发送的参考截图没有增加“完全照着该图制作”的授权。曾提出的“图钉和四向箭头切换”已经被后续“同一图钉亮暗切换”的要求替代。

本次操作要求：将本任务代码回退到 15:19 消息之前，同时整理以上需求。该消息之后的本任务代码已另存备份；另一任务的 GUI 与图标设计工作保留。portable 原本尚未更新为这些试改，不需再次覆盖，个人设置与 local-data 保留。
